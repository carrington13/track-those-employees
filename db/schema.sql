

CREATE TABLE departments ();

CREATE TABLE roles (
    role_id INTEGER AUTO_INCREMENT PRIMARY KEY
    title VARCHAR(30) NOT NULL;
    salary INTEGER NOT NULL;
    -- department TEXT MAKE FOREIGN KEY


);

CREATE TABLE employees ();